Nature Fit public assets
------------------------

This folder holds Nature Fit–specific static assets for the Next.js app.

Expected files:
- logo-primary.jpg  - primary logo used in header and footer
- logo-mark.jpg     - secondary mark or icon logo

You can replace or update these images, but keep the same filenames
or update any React components that reference them (for example,
`SiteShell.tsx` currently uses `/nature-fit/logo-primary.jpg`).

